{
  "name": "release-notes-drafter-action",
  "version": "1.0.0",
  "description": "Automatically generate release notes from commit history",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && ncc build dist/index.js -o dist --license licenses.txt",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "keywords": [
    "github-action",
    "release",
    "changelog",
    "commits",
    "automation"
  ],
  "author": "Gaoruan Tech <hello@gao-soft.com>",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vercel/ncc": "^0.38.0",
    "typescript": "^5.0.0"
  }
}
